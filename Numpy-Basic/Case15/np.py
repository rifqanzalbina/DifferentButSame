# Import Library
import numpy as np

# --- Codes ---

# NumPy Program to return random integers from the values specified with low (inclusive) to high (exclusive) param
x = np.random.randint(1,20,50)
print("Values of random randint array : ")
print(x)

# Numpy Program to creat an array of specified shape and fills it with random values from a normal (Gaussian) distribution.

x1 = np.random.normal(1,20,50)
print("Values of random normal array : ")
print(x1)

# --- EndCodes ---
