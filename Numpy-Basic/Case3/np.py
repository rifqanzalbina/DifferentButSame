# Import the Library
import numpy as np

# Test if any of the elements of a given array are non-zero.

# --- Code ---

x = np.array([1,0,0,3])
print("A Sample Array")
print(x)

print("Test program if any of the elements of a given array are non-zero.")
print(np.any(x)) # function that checks if any element in an array evaluates to True

print()

y = np.array([0,0,0,0])
print("A Sample Array")
print(y)

print("Test program if any of the elements of a given array are non-zero.")
print(np.any(y))

# --- EndCode --- 



